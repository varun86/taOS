# tinyagentos.otel — OpenTelemetry integration package.
# Phase 1: span_store (per-agent otel-spans.db + helpers).
# Phase 2: emitter + receiver.
# Phase 3: AgentTracesPanel frontend (desktop/src/apps/agents/).
# Phase 4: judge (post-hoc reasoning audit, fires on session_end).
# Phase 5: BenchmarkApp + routes/benchmarks.py proxy (pending).
